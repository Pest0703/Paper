# PaperTutor 0.2.1 测试报告

本报告记录实际执行结果，而非功能清单。测试环境：Windows，Electron 37，Node.js 24。

## OCR 独立文本页与选区提问

- OCR 输出已从右侧导师回答区拆离，显示在左侧论文与右侧导师之间的可关闭文本页；用户也可在尚未识别时手动打开并看到“未进行OCR”。
- 每次 OCR 开始会自动打开中间页并显示识别状态；完成后按页展示可选文字，多页按页码排序，重复识别同页时替换而不重复堆叠。
- 中间页文字选取使用蓝色高亮，选区进入原右侧“编辑选中内容”框；选择动作不会自动调用模型，仍需用户确认提问。
- OCR 原始识别内容不再写入 `answer` 或 `TutorPanel`。真实流程验证右侧回答数量为 0，同时 OCR 选区成功进入导师编辑框。
- 真实 OCR 回归记录 3 次：2 次真实调用、1 次本地缓存命中；中间页可见，书签和阅读页码保留，状态文件无明文 API Key。
- 新增 3 项 OCR 工作区单元测试；当前单元测试 57/57 通过，Electron UI 2 项通过、1 项因私人 fixture 未提供而跳过，Production Build 通过。

## Vision / OCR 设置页能力探测修复

- 根因：旧测试使用 1×1 PNG，部分多模态后端会将其作为无效图片拒绝；旧错误分类又把所有包含 image/vision/multimodal 的 HTTP 400 统一误报为“不支持图片输入”。
- 新测试在内存 Canvas 中生成 320×200 匿名 PNG：VISION 图包含 `VISION TEST / 123`，OCR 图包含 `OCR TEST / 456`，不落盘、不持久化、不记录 Base64。
- VISION 只有在 HTTP 成功、正文非空且包含 `123` 时显示“视觉能力测试通过”；OCR 独立要求返回包含 `456`。仅连接成功但未识别预期内容时显示“未能确认能力”。
- HTTP 400 现区分 `vision_unsupported`、`image_invalid`、`image_size`、`image_format`、`bad_request`；详细错误在主进程脱敏后才返回设置页。
- 当前用户配置下真实低成本探测各执行一次：`qwen3.8-flash：视觉能力测试通过`；`qwen3.8-flash：OCR 测试通过`。
- 单元测试 51/51 通过，Electron UI 2 项通过、1 项因无仓库论文样本跳过，Production Build 通过。真实截图和论文 OCR 业务代码未修改。

## 0.2.1 Beta 三路 API、OCR 与缓存治理

- TEXT、VISION、OCR 均有独立 URL、API Key、Model、安全存储槽和测试入口；凭据选择单元测试验证三路不串用，缺失配置时不回退。
- 概述 Prompt 与导师核心 Prompt 默认要求简体中文，同时保留专业名词、数据集名、公式、指标和缩写；用户明确指定其他语言时允许覆盖。
- OCR 仅由用户点击“对此页执行 OCR”触发，使用当前页 PNG；原生 PDF 文字不被覆盖，识别结果以 `source: ocr` 追加并按论文、页码、图像哈希、模型和 Prompt 版本缓存。
- 真实 OCR 流程共记录 3 次：2 次真实 `qwen3.8-max` 调用、1 次本地缓存命中；清除缓存后再次调用成功。上下文查看器隐藏 Base64 并展示 OCR 输出来源。
- 缓存页分别统计 AI 回答、OCR 和 Word 转换缓存大小；清理前确认，清理后报告释放量，占用文件跳过。自动化重启确认 API 配置与密钥仍在，论文阅读页码和书签不变。
- 当前全量单元测试 48/48 通过；Electron UI 2 项通过、1 项因仓库按安全规则不纳入论文文件而跳过；生产构建通过。
- 当前真实模型回归：`qwen3.7-plus` TEXT 为 635 tokens、约 3.3 秒；`qwen3.8-max` VISION 为 1,236 tokens、约 14.9 秒；两次请求 ID 独立，图片 Base64 未进入查看器。

## AI 调用可观测性与上下文查看器

- 单次回答显示真实请求模型、TEXT/VISION、输入/输出/总 Token、总耗时、可用时的首字延迟、本地缓存和服务商缓存。
- Streaming 请求发送 `stream_options.include_usage=true`，最终 usage chunk 会合并到请求结果；服务商未返回 usage 时显示“不可用”，不会伪装成 0。
- `qwen3.7-plus` 按基础公开原价 2/8 元每百万输入/输出 Token，`qwen3.8-max` 按 12/36 元估算；未知模型不估价。
- 快照在最终 messages 构造完成、IPC 调用前生成，并通过 requestId 与对应回答绑定；连续问题不会互相覆盖。
- 视觉快照只保留已附加、image/png、像素尺寸和页码，最终 messages 中的 Base64 被替换为安全说明。
- 复制功能复用同一安全清洗器，过滤 API Key、Authorization、Cookie、密码、Secret、访问令牌和 Bearer 内容。
- 长内容默认折叠，单块最多渲染 12,000 字符，查看器使用独立滚动容器。
- 新增测试后单元测试为 41/41；Electron UI 为 2 项通过、1 项因仓库不包含测试论文而跳过；生产构建通过。

## 自动检查范围

- TypeScript 与生产构建
- Context Builder 的句段章节定位、全文相关段落检索、证据边界提示
- Electron 真实窗口启动
- 800×600、1024×768、1280×720、1366×768、1440×900、1920×1080、2560×1440
- 设置页布局与 API 配置控件
- 真实可提取文本 PDF 的导入
- 关闭并重新启动后的论文记录恢复

## 本轮修复记录

- PDF.js worker 缺少 Vite 类型声明：增加 Vite 客户端类型。
- 开发模式没有明确服务地址：Electron 非打包模式固定连接本机 Vite 服务。
- 流监听与初始化混在一个依赖变化的 effect：拆分，避免每次请求重新加载应用状态。
- 未配置 Key 时导入记录未立即落盘：在转到设置页前保存结构档案。
- PDF.js 5 与 Electron 37 的二进制 API 不兼容：切换到 PDF.js 4.10。
- 加密密钥被普通设置覆盖：改为独立 `encryptedSecret` 字段。
- 初始化时快速操作会触发状态竞态：增加 hydration 启动门控。
- 兼容接口的流式结束处理不完整：加入响应类型探测、`[DONE]` 退出和最终全文回填。
- PDF 文本层原本几乎透明：恢复可见文本选区，选中句子时以 Windows 风格蓝色高亮显示。
- 单页翻页改为 24 页连续文档流，鼠标滚轮可直接跨页，页码随阅读位置更新。
- DeepSeek 新模型默认思考模式可能耗尽输出配额：阅读解释请求显式关闭思考模式，避免只有 `reasoning_content` 而无正文。

## 文字与图片模型独立路由

- 文字模型固定为 `qwen3.7-plus`，图片理解模型固定为 `qwen3.8-max`；两者共享百炼 OpenAI-compatible Base URL 与加密保存的 API Key。
- 旧版 `model` 仅迁移至 `textModel`，不会填入 `visionModel`；图片模型为空时直接提示，不会回退到文字模型。
- 文字解释、追问、Prompt Engine 和导入预读只进入 `TEXT`；截图、公式、图表和图片只进入 `VISION`。
- OpenAI-compatible 图片消息实际使用 `text + image_url(data:image/png;base64,...)` 内容数组；缓存身份加入路由和实际模型。
- 34 项单元测试全部通过；Electron 界面在 7 种尺寸下通过，两套模型互不串改，保存后重启分别恢复。
- 真实百炼最小请求各执行 1 次：文字模型 HTTP 200，约 1.2 秒返回；图片模型 HTTP 200，约 5.5 秒返回。测试输出未包含 API Key、请求正文或 base64 图片。

## 明确未纳入

按产品要求，不提供本地模型或本地生成答案；未配置 API Key 时不声称模型解释通过。本轮不执行断网专项测试。

## 已知限制

复杂双栏、扫描件、图表视觉语义和公式二维结构仍需要后续增强。其余 3 个单元用例与 2 个 Electron UI 流程均通过。

## 6.0 中文综述专项测试

- 输入：一份未提交仓库的 24 页私人 DOCX，只读转换为临时 PDF 测试副本。
- Word 与 PDF 均为 24 页；PDF 24 页均含可提取文字，共约 57,792 个字符。
- 人工检查第 1、12、24 页：标题、摘要、双栏正文、小节标题、引文列表和页码均正常，无裁切或重叠。
- PaperTutor 导入、结构解析、真实 `deepseek-flash` 预读和 Overview 生成成功。
- 重启后恢复到第 12 页；该页文本层包含 269 个可选择文字块。
- 第 12 页可见选区为蓝色高亮；选中 97 个字后，真实 `deepseek-flash` 在 5 秒内返回 915 字符的完整中文解释。
- 连续滚动测试一次向下滚动 1400 像素，页面从第 12 页顺势进入第 14 页，无需点击下一页。

## Word 导入与 PDF 选句补全

- 支持 `.doc` / `.docx`；Windows 上使用 Microsoft Word 将本地临时副本导出为 PDF 阅读缓存，原文以只读方式打开并且关闭时不保存。
- 实测通过环境变量传入 `private-test-paper.docx`，解析 24 页；转换前后原文 SHA-256 一致。
- 故意将句子末端截断：鼠标原始选区 28 字，发送前恢复为 38 字完整句，包含句末标点；DeepSeek 返回 399 字解释。
- 短术语选择保持原样，不会被自动扩成整句。

## Prompt v2 任务专属输出

- 仅 AUTO 允许①—⑥六项结构；EXAMPLE、WHY_HERE、FORMULA 和 FOLLOWUP 均有独立 `[OUTPUT]` 契约。
- 真实 DeepSeek 复测：EXAMPLE 仅输出“最小具体例子—映射—解决的难点”，未重复六项；WHY_HERE 仅输出逻辑角色、承接、推动与删除损失；FOLLOWUP 仅解释所问第三点。
- AUTO 的“意思”复测补出了“数据条件不同 → 实验条件不同 → 分数不可比 → 必须记录构建细节”的隐含因果链，不再只调换原文语序。
- 实测输出结构断言已写入 `tests/prompt-engine-live.mjs`：AUTO 必须包含六项，非 AUTO 如果重复六项则测试直接失败。

## 可编辑选区与确认重分析

- 论文选区会进入右侧可编辑文本框，支持补字、删字、删除误选内容或整体重写；修改状态使用浅黄色背景明确提示。
- 提供“恢复原始选区”和“按修改内容重新分析”两个操作；模型回答期间锁定编辑框，避免显示文字与回答依据不一致。
- 真实桌面流程实测：原始选区 52 字，人工改为 44 字；纯编辑未产生 API 请求，恢复后与原选区逐字一致。
- 点击确认后仅新增 1 次 DeepSeek 请求，返回 382 字有效解释；请求指标中未记录 API Key。
- 自动化证据：`tests/edit-selection-live.mjs`、`test-assets/edit-selection-live-results.json`、`screenshots/editable-selection.png`。

## 阅读位置、导师跟随与多书签

- 阅读状态改为同步合并页码与滚动高度，避免连续滚动时两个保存动作互相覆盖；每篇论文独立保存。
- 真实关闭重启测试：关闭前第 13 页、滚动高度 10966.667；重新打开后页码仍为 13，滚动高度仍为 10966.667。
- 左侧可输入名称添加多个书签，并可删除；书签记录页码和页内精确位置，重启后仍保留。
- 书签跳转实测：从其他位置点击第 7 页书签，页码与滚动高度 5665.333 均精确恢复。
- 导师追问实测：回答完成后自动滚至最新内容，底部间距约 0.67 像素，无需手动下滑。
- 自动化证据：`tests/reading-state-bookmarks-live.mjs`、`tests/tutor-autoscroll-live.mjs`、`test-assets/reading-state-bookmarks-results.json`。

## 公式与图表截图提问

- 阅读工具栏新增“截图提问”；启用后可在任意 PDF 页面拖动框选公式、图表或图片，松开鼠标即生成 PNG 截图。
- 右侧导师面板显示截图预览和可编辑问题，用户确认后才发送，不会在拖框时自动消耗模型额度。
- 请求同时携带截图、所在页码、该页附近正文和任务专属提示；截图只使用设置页的图片理解模型，普通文字请求只使用文字模型。
- 视觉模型不支持图片时会显示明确错误，不再归类为无说明的网络失败。
- 真实 PDF 界面测试生成 107,266 字符的 PNG data URL，截图预览、问题编辑和发送入口均通过；28 项既有单元测试继续通过。

## 选区透明度与调用确认

- PDF 文字选中高亮改为 38% 透明度的 Windows 风格蓝色，能明确看到选区范围，同时保留底层文字可读性。
- 拖选文字后只执行句尾补全并填入右侧修改框，清空旧回答但不调用 API；用户可先增删修正，再点击“确认内容并分析”。
- 截图提问同样先显示预览和问题编辑框，点击“确认截图并分析”后才发送图片。
- 真实 PDF 自动化验证：文字识别内容成功进入编辑框，确认前模型请求数为 0；截图生成、问题编辑和确认按钮均正常。

## 自定义模型服务与提示词

- 设置页提供 DeepSeek、OpenAI 和“自定义（OpenAI Compatible）”；自定义模式可独立指定 Base URL、API Key 与 Model。
- 请求继续使用标准 `/chat/completions`、Bearer Key、messages、temperature、max_tokens 和 stream 字段。
- 自定义提示词从设置页移至选区后的右侧导师面板；可直接选择“自定义提问”，不会要求离开阅读页。
- 选区修改框增加“综合精读、只解答疑问、深层解释、举一个例子、公式解释”模板和可自由修改的本次提示词。
- 选择文字时不调用模型；用户可直接写下唯一疑问，点击确认后文字、上下文和该问题在一次请求中发送。
- 自动化检查通过：自定义服务字段均可编辑，本次提示词可修改，确认前模型请求为 0；28 项单元测试通过。
