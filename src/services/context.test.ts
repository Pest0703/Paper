import{describe,expect,it}from'vitest';import{buildPrompt,completeSelection,findContext,retrieve}from'./context';import type{PaperProfile}from'../types';
const profile:PaperProfile={title:'Entity Alignment',authors:'A',abstract:'',researchQuestion:'How to align entities?',contributions:['Relation-aware matching'],methods:['Graph encoder'],keywords:[],sections:[{id:'s1',title:'3 Method',level:1,page:2,summary:'A relation-aware graph encoder.',paragraphs:[{id:'p1',text:'We encode relation semantics into entity representations.',page:2,sectionId:'s1',role:'提出方法',sentences:[{id:'x',text:'We encode relation semantics into entity representations.',page:2,paragraphId:'p1',sectionId:'s1'}]},{id:'p2',text:'Beijing and China form a capital relation.',page:2,sectionId:'s1',sentences:[]}]}]};
describe('context builder',()=>{it('traces a selection to its hierarchy',()=>{const c=findContext(profile,'relation semantics');expect(c.section.title).toBe('3 Method');expect(c.paragraph.id).toBe('p1');expect(c.sentence?.id).toBe('x')});it('retrieves related paper paragraphs',()=>expect(retrieve(profile,'Beijing capital')[0].id).toBe('p2'));it('includes evidence boundaries and paper context',()=>{const x=buildPrompt(profile,'entity representations');expect(x.messages[0].content).toContain('证据不足');expect(x.messages[1].content).toContain('完整句')});it('restores a sentence whose PDF selection lost its tail',()=>expect(completeSelection(profile,'We encode relation semantics into entity')).toBe('We encode relation semantics into entity representations.'));it('does not expand a short selected term',()=>expect(completeSelection(profile,'relation')).toBe('relation'))});
